import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // evita instanciación

  static const background = Color(0xFFFFF2EC);
  static const primary = Color(0xFFAC7099);
  static const secondary = Color(0xFFEED7E7);
  static const inputFill = Colors.white;
  static const labelText = Colors.black54;
  static const buttonText = Colors.white;
}
